<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                   xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.5.xsd">

  <changeSet id="create_schema_store" author="">
    <sql>
      create schema if not exists store;
    </sql>
  </changeSet>

  <changeSet id="default_privileges_for_schema_store" author="">
    <sql>
      GRANT USAGE ON SCHEMA store TO kefhir_app;
      ALTER DEFAULT PRIVILEGES IN SCHEMA store GRANT USAGE ON SEQUENCES TO kefhir_app;
      ALTER DEFAULT PRIVILEGES IN SCHEMA store GRANT EXECUTE ON FUNCTIONS TO kefhir_app;
      ALTER DEFAULT PRIVILEGES IN SCHEMA store GRANT SELECT,INSERT,UPDATE ON TABLES TO kefhir_app;
    </sql>
  </changeSet>

</databaseChangeLog>

